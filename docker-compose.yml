services:
  mongo:
    # MongoDB 5.0+ requires CPU AVX; use 4.4 on older hosts (e.g. HP Z800 / pre-AVX Xeon).
    # On AVX-capable hardware you can switch to mongo:7 (new data dir / dump-restore if upgrading).
    image: mongo:4.4
    ports:
      - "27017:27017"
    volumes:
      - vrika_mongo:/data/db
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mongo --quiet --eval 'quit(db.runCommand({ ping: 1 }).ok ? 0 : 2)'",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - vrika_redis:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  api:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./server/.env
    environment:
      MONGODB_URI: mongodb://mongo:27017
      REDIS_URL: redis://redis:6379/0
      # Agent runs separately (~/nyxstrike docker compose); API reaches it on the host.
      AGENT_MICROSERVICE_URL: http://host.docker.internal:8888
      AGENT_TOOL_STREAM_REDIS_URL: redis://redis:6379/0
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      mongo:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  web:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      PY_API_URL: http://api:8000
      INTERNAL_API_URL: http://api:8000
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

volumes:
  vrika_mongo:
    name: vrika_mongo

  vrika_redis:
    name: vrika_redis

networks:
  default:
    name: vrika-network